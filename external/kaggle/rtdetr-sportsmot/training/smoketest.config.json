{
  "runner": "kaggle-kernel",
  "slug": null,
  "kernel_metadata": {
    "language": "python",
    "kernel_type": "notebook",
    "is_private": true,
    "enable_gpu": true,
    "machine_shape": "NvidiaTeslaT4",
    "enable_internet": true,
    "dataset_sources": [
      "external-secrets"
    ],
    "kernel_sources": [
      "rtdetr-sportsmot-training-prepare-data"
    ],
    "competition_sources": [],
    "model_sources": []
  },
  "notes": [
    "Identical mounts to index.config.json on purpose: the smoke test's job is to validate, in a few minutes, exactly the environment the real trainer will see (GPU/torch kernels, deps, HF token, staged-data mount + untar, mock training).",
    "slug=null -> the kaggle service runner derives the kernel name from the <type>-<name> convention. Kernels live in a Kaggle collection named after the model (maintained in the UI \u2014 the API has no collections endpoint).",
    "Derived slug: <model>-smoketest."
  ],
  "timeout_seconds": null
}